/// <reference types="node" />
import { IProcessorStateHandler } from './IProcessorStateHandler';
import { EventEmitter } from 'events';
import { IProcessorState } from './IProcessorState';
export declare class ProcessorStateHandler extends EventEmitter implements IProcessorStateHandler {
    readonly processorID: string;
    constructor(processorID?: string);
    persist(state: IProcessorState): Promise<void>;
    init(atBlock?: number): Promise<IProcessorState>;
}
