import { RedisOptions } from 'ioredis';
import * as IORedis from 'ioredis';
export declare class RedisClientFactory {
    private clients;
    private factoryMetod;
    constructor(redisURI?: string, options?: RedisOptions);
    getClient(): IORedis.Redis;
    stop(): void;
}
