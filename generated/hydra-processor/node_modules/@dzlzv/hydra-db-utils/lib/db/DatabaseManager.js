"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.makeDatabaseManager = void 0;
const helper = require("./helper");
/**
 * Create database manager.
 * @param entityManager EntityManager
 */
function makeDatabaseManager(entityManager) {
    return {
        save: async (entity) => {
            entity = helper.fillRequiredWarthogFields(entity);
            await entityManager.save(entity);
        },
        remove: async (entity) => {
            await entityManager.remove(entity);
        },
        get: async (entity, options) => {
            return await entityManager.findOne(entity, options);
        },
        getMany: async (entity, options) => {
            return await entityManager.find(entity, options);
        },
    };
}
exports.makeDatabaseManager = makeDatabaseManager;
//# sourceMappingURL=DatabaseManager.js.map