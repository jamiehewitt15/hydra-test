import { ApiOnlyWhereArgs } from '../../generated';
import { ApiOnly } from './api-only.model';
import { ApiOnlyService } from './api-only.service';
export declare class DishResolver {
    readonly service: ApiOnlyService;
    constructor(service: ApiOnlyService);
    dishes({ where, orderBy, limit, offset }: ApiOnlyWhereArgs, fields: string[]): Promise<ApiOnly[]>;
}
