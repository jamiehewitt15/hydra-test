import { DeepPartial, Repository, EntityManager } from 'typeorm';
import { BaseService } from '../../../';
import { Dish } from './dish.model';
export declare class DishService extends BaseService<Dish> {
    protected readonly repository: Repository<Dish>;
    constructor(repository: Repository<Dish>);
    successfulTransaction(data: DeepPartial<Dish>, userId: string, manager?: EntityManager): Promise<Dish[]>;
    failedTransaction(data: DeepPartial<Dish>, userId: string, manager?: EntityManager): Promise<Dish[]>;
}
