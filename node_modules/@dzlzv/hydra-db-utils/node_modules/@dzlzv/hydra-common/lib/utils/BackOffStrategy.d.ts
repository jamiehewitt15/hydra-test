export interface BackoffStrategy {
    /**
     * reset the backoff time to it's initial value
     */
    resetBackoffTime(): void;
    /**
     * Get current backoff time
     */
    getBackOffMs(): number;
    /**
     * Indicate that a failure has occured and the backoff time should be increased
     */
    registerFailure(): void;
}
export declare class ConstantBackOffStrategy implements BackoffStrategy {
    readonly waitTime: number;
    constructor(waitTime: number);
    resetBackoffTime(): void;
    registerFailure(): void;
    getBackOffMs(): number;
}
export declare class ExponentialBackOffStrategy implements BackoffStrategy {
    private backoffTime;
    maxBackOffTime: number;
    resetBackoffTime(): void;
    registerFailure(): void;
    getBackOffMs(): number;
}
