"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.numberEnv = void 0;
const debug_1 = require("debug");
const debug = debug_1.default('index-builder:processor');
// get a number from an environment variable
function numberEnv(envVariable) {
    if (!process.env[envVariable]) {
        return undefined;
    }
    let value;
    try {
        value = Number.parseInt(process.env[envVariable] || '');
        debug(`Using ${envVariable}: ${value}`);
    }
    catch (e) {
        console.error(`Cannot parse env ${envVariable} value ${process.env[envVariable] || ''} into a number`);
    }
    return value;
}
exports.numberEnv = numberEnv;
//# sourceMappingURL=env-flags.js.map