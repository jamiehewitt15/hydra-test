import { CodegenFlags } from '../commands/codegen';
export default class WarthogWrapper {
    readonly flags: CodegenFlags;
    private readonly schemaResolvedPath;
    private prod;
    constructor(flags: CodegenFlags, prod?: boolean);
    run(): Promise<void>;
    generateDB(): Promise<void>;
    newProject(projectName?: string): Promise<void>;
    prepareProjectFiles(): void;
    installDependecies(): Promise<void>;
    createDB(): Promise<void>;
    getWarthogDependency(): string;
    /**
     * Generate the warthog source files:
     *   - model/resolver/service for entities
     *   - Fulltext search queries (migration/resolver/service)
     */
    generateWarthogSources(): void;
    codegen(): Promise<void>;
    syncSchema(): Promise<void>;
    runMigrations(): Promise<void>;
    updateDotenv(): Promise<void>;
}
