import { FTSQuery } from '../model';
import { GeneratorContext } from '../generate/SourcesGenerator';
export declare class FTSQueryRenderer {
    private _context;
    constructor(context?: GeneratorContext);
    generate(mustacheTeplate: string, query: FTSQuery): string;
    private transform;
    private objectTypeToMustache;
    private name2column;
    private name2table;
    private queryName2prefix;
    private name2modelName;
    private fieldName;
    private arrayName;
}
