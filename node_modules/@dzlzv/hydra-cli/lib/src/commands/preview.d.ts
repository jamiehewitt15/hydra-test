import { Command, flags } from '@oclif/command';
export default class Preview extends Command {
    static description: string;
    static flags: {
        schema: flags.IOptionFlag<string>;
    };
    run(): Promise<void>;
    generateAPIPreview({ schema, generatedFolderPath, isGeneratedFolderPathExists, }: {
        schema: any;
        generatedFolderPath: any;
        isGeneratedFolderPathExists: any;
    }): Promise<void>;
}
