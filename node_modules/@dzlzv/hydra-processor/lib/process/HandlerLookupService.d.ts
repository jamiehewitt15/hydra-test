import { ProcessorOptions, EventHandlerFunc } from '../start';
export declare class HandlerLookupService {
    protected options: ProcessorOptions;
    private _events;
    private _event2mapping;
    private _processingPack;
    private _translator;
    constructor(options: ProcessorOptions);
    eventsToHandle(): string[];
    lookupHandler(eventName: string): EventHandlerFunc;
}
