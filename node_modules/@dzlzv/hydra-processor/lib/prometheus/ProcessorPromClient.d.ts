import { Gauge } from 'prom-client';
import { MappingsProcessor } from '../process';
import { IProcessorStateHandler } from '../state';
export declare class ProcessorPromClient {
    protected stateHandler: IProcessorStateHandler;
    protected processor: MappingsProcessor;
    protected lastScannedBlock: Gauge<string>;
    protected processedEvents: Gauge<"name">;
    constructor();
    init(): void;
    private initValues;
}
