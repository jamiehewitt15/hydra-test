/// <reference types="node" />
import { IProcessorSource, EventFilter } from './';
import { SubstrateEvent } from '@dzlzv/hydra-common';
import { ProcessorOptions } from '../start';
import { EventEmitter } from 'events';
export declare class GraphQLSource extends EventEmitter implements IProcessorSource {
    protected options: ProcessorOptions;
    private graphClient;
    constructor(options: ProcessorOptions);
    subscribe(events: string[]): Promise<void>;
    indexerHead(): Promise<number>;
    nextBatch(filter: EventFilter, size: number): Promise<SubstrateEvent[]>;
}
